package serial;

import java.awt.*;

public class Parser
{
    public static String colorToString(Color c)
    {
        // return (c.getRed() + ", " + c.getGreen() + ", " + c.getBlue());
        return c.getRed() + "";
    }
}
